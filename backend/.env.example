# Supabase Configuration
# Get these values from your Supabase project dashboard
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# API Configuration
VITE_API_URL=http://localhost:6868

# Optional: Backend Configuration
VITE_BACKEND_URL=http://localhost:6868

# Copy these lines into a new file named .env or .env.local in the project root
# .env and .env.local are git-ignored; they will override these defaults during dev
